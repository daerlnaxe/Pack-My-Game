<Page x:Class="UnPack_My_Game.Graph.LaunchBox.P_LBMethod"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UnPack_My_Game.Graph.LaunchBox"
      xmlns:l="clr-namespace:UnPack_My_Game.Resources"      
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="P_LBMethod">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- Methode -->
        <TextBlock Text="{x:Static l:Lang.I_LaunchboxMethod}" Grid.ColumnSpan="3"/>
        <!-- Plateforme -->
        <Label Grid.Row="1" Content="{x:Static l:Lang.Platform}"/>
        <ComboBox Grid.Row="1" Width="200" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="1"
                          ItemsSource="{Binding Machines}" DisplayMemberPath="Name"
                          SelectedValue="{Binding SelectPlatform, NotifyOnValidationError=True}" />
    </Grid>
</Page>
